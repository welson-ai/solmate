{
  "name": "solmate-contracts",
  "version": "1.0.0",
  "description": "Solmate smart contracts for Celo",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network celo",
    "verify": "hardhat verify",
    "spin": "hardhat run scripts/spin-transactions.js --network celo"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
